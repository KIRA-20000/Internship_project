{
  "scripts": {
    "backend": "npm start --prefix Back_end",
    "frontend": "npm run dev --prefix Front_end",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}